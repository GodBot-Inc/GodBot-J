package com.godbot;

import com.godbot.utils.customExceptions.LinkInterpretation.InvalidURLException;
import com.godbot.utils.customExceptions.LinkInterpretation.RequestException;

import java.io.IOException;

public class Test {
    public static void main(String[] args) throws InterruptedException, IOException, RequestException, InvalidURLException {
//        BasicConfigurator.configure();
//        AsyncHttpReceiver handler = new AsyncHttpReceiver();
//        AsyncHttpReceiver handler2 = new AsyncHttpReceiver();
//        long start = System.currentTimeMillis();
//        LinkHelper.sendAsyncRequest(
//                url,
//                handler
//        );
//        LinkHelper.sendAsyncRequest(
//                url,
//                handler2
//        );
//        while (handler.getCompleted() + handler.getCompleted() != 2) {
//            TimeUnit.MILLISECONDS.sleep(10);
//        }
//        System.out.println("First: " + (System.currentTimeMillis()-start));
//        System.out.println(handler.getResponses());
//        System.out.println(handler2.getResponses());
//
//        start = System.currentTimeMillis();
//        LinkHelper.sendRequest(url);
//        LinkHelper.sendRequest(url);
//        System.out.println("Second: " + (System.currentTimeMillis() - start));
    }
}
